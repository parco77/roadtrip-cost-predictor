__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
frontend/node_modules/
frontend/dist/
.vite/
.DS_Store

# Large, fully reproducible model artefacts.
# models/pipeline.joblib IS committed - app.py needs it and it is small once the
# evaluation-only forests are kept out of it (see scripts/train_pipeline.py).
# These two are rebuilt by re-running their scripts and are not needed to read the notebook:
models/pipeline_eval.joblib
models/end_to_end.joblib
