__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store

frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.vercel/
# Build-time config, per machine. frontend/.env.example is the committed template.
frontend/.env
frontend/.env.local

# Evaluation-only bundle: several hundred MB of forests that exist to be scored, never served.
# backend/models/*.joblib ARE committed - the API loads them at import and Render installs no
# training dependencies, so they have to arrive with the repo.
ml/models/pipeline_eval.joblib
