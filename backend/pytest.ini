# tests/ has no __init__.py, so pytest puts tests/ on sys.path and not this directory -
# `import app` then fails under a bare `pytest` while passing under `python -m pytest`,
# which adds the working directory itself. Naming the path here makes both work.
[pytest]
pythonpath = .
testpaths = tests
